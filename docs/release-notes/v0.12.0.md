## Callisto `v0.12.0`

One-click **chain switching** across Ethereum and the major L2s, a curated **Build** tab for Safe multisig transactions, and a big **RPC-efficiency** pass (Multicall3 batching) so public endpoints stay usable.

### Install

**macOS** — download `Callisto-v0.12.0-darwin-arm64.zip` (Apple silicon) or
`-darwin-amd64.zip` (Intel), unzip, and move **Callisto.app** to /Applications.
First launch: right-click → **Open** (once), or
`xattr -dr com.apple.quarantine /Applications/Callisto.app`.

**Linux** — download `Callisto-v0.12.0-linux-amd64.tar.gz` and extract.

Already running Callisto? Just use **Settings → Check for updates** — it verifies
and installs this release for you.

### Verify (optional)

```
shasum -a 256 -c SHA256SUMS
```

`SHA256SUMS` is ed25519-signed as `SHA256SUMS.sig` with the maintainer key (the
same key the in-app updater checks).

### Changes

See the [`v0.12.0` changelog](https://codeberg.org/pasiphae/callisto/src/branch/main/CHANGELOG.md#0120---2026-07-20)
for the full list.
